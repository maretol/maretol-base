{
  "name": "cms-data-fetcher",
  "version": "0.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "deploy-prd": "wrangler deploy",
    "deploy-stg": "wrangler deploy -e staging",
    "dev": "wrangler dev",
    "dev-bg": "wrangler dev > wrangler_dev.log",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250422.0",
    "typescript": "^5.8.3",
    "vitest": "3.1.2",
    "wrangler": "^4.12.1"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "microcms-js-sdk": "^3.2.0"
  }
}
