{
  "name": "sns-article-publisher",
  "version": "0.0.0",
  "private": true,
  "description": "SNSに記事を投稿する処理",
  "main": "index.js",
  "scripts": {
    "deploy-prd": "wrangler deploy",
    "deploy-stg": "wrangler deploy -e staging",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest run",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@atproto/api": "^0.18.1",
    "oauth": "^0.10.2"
  },
  "devDependencies": {
    "@types/oauth": "^0.9.6",
    "api-types": "^1.0.0",
    "nostr-tools": "^2.17.3",
    "vitest": "^4.0.10"
  }
}
